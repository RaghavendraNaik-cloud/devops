pipeline{
    agent any
        tools {
            maven 'maven'
            jdk 'java_11'
        }
        stages{

        stage("git checkout"){
            steps{
                git branch: 'main', url: 'https://github.com/RaghavendraNaik-cloud/My-cloud.git' 
            }
        }
        stage ("maven clean"){
            steps{
                sh 'mvn clean'
            }
        }
        stage("maven build"){
            steps{
                sh 'mvn build'
            }
        }

    }
}

    


    
